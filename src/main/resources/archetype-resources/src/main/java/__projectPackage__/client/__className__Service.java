package ${groupId}.${projectPackage}.client;


import ${groupId}.${projectPackage}.dto.SimpleDTO;

import java.util.List;

public interface ${className}Service {

	public SimpleDTO sayHello(String name);

}
